--==================================================
-- 🐺 DOUBLEX 🐺
-- GUI MOBILE V6
-- UM ÚNICO LOCALSCRIPT
--==================================================

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")

local Player = Players.LocalPlayer


--==================================================
-- GUI
--==================================================

local Gui = Instance.new("ScreenGui")
Gui.Name = "DOUBLEX_UI"
Gui.ResetOnSpawn = false
Gui.IgnoreGuiInset = true
Gui.Parent = Player:WaitForChild("PlayerGui")



--==================================================
-- FUNÇÃO ARRASTAR
--==================================================

local function DragObject(Object, MoveObject)

	local Dragging = false
	local StartPos
	local StartObjectPos


	Object.InputBegan:Connect(function(Input)

		if Input.UserInputType == Enum.UserInputType.Touch then

			Dragging = true

			StartPos = Input.Position
			StartObjectPos = MoveObject.Position

		end

	end)



	UserInputService.InputChanged:Connect(function(Input)

		if Dragging and Input.UserInputType == Enum.UserInputType.Touch then

			local Delta = Input.Position - StartPos


			MoveObject.Position =
				UDim2.new(
					StartObjectPos.X.Scale,
					StartObjectPos.X.Offset + Delta.X,

					StartObjectPos.Y.Scale,
					StartObjectPos.Y.Offset + Delta.Y
				)

		end

	end)



	UserInputService.InputEnded:Connect(function(Input)

		if Input.UserInputType == Enum.UserInputType.Touch then

			Dragging = false

		end

	end)

end



--==================================================
-- BOLHA
--==================================================

local Bubble = Instance.new("TextButton")

Bubble.Size =
	UDim2.new(0,65,0,65)

Bubble.Position =
	UDim2.new(0,30,0.5,0)

Bubble.Text =
	"🐺"

Bubble.TextScaled = true

Bubble.Parent = Gui



local BubbleCorner = Instance.new("UICorner")
BubbleCorner.CornerRadius = UDim.new(1,0)
BubbleCorner.Parent = Bubble



--==================================================
-- PAINEL
--==================================================

local Panel = Instance.new("Frame")

Panel.Size =
	UDim2.new(0,300,0,380)

Panel.Position =
	UDim2.new(0,120,0.25,0)

Panel.Visible = false

Panel.Parent = Gui



local PanelCorner = Instance.new("UICorner")
PanelCorner.CornerRadius = UDim.new(0,15)
PanelCorner.Parent = Panel



--==================================================
-- BARRA SUPERIOR ARRASTÁVEL
--==================================================

local Header = Instance.new("TextButton")

Header.Size =
	UDim2.new(1,0,0,50)

Header.Position =
	UDim2.new(0,0,0,0)

Header.Text =
	"🐺 DOUBLEX 🐺"

Header.TextScaled = true

Header.BackgroundTransparency = 1

Header.Parent = Panel



--==================================================
-- MINIMIZAR
--==================================================

local Minimize = Instance.new("TextButton")

Minimize.Size =
	UDim2.new(0,40,0,40)

Minimize.Position =
	UDim2.new(1,-45,0,5)

Minimize.Text =
	"−"

Minimize.TextScaled = true

Minimize.Parent = Panel



--==================================================
-- STATUS
--==================================================

local Status = Instance.new("TextLabel")

Status.Size =
	UDim2.new(1,-20,0,35)

Status.Position =
	UDim2.new(0,10,0,60)

Status.Text =
	"Status: Pronto"

Status.TextScaled = true

Status.BackgroundTransparency = 1

Status.Parent = Panel



--==================================================
-- BOTÕES
--==================================================

local function CreateButton(Text,Y)

	local Button = Instance.new("TextButton")

	Button.Size =
	UDim2.new(0.8,0,0,45)

	Button.Position =
	UDim2.new(0.1,0,0,Y)

	Button.Text =
	Text

	Button.TextScaled = true

	Button.Parent = Panel

	return Button

end



local Quest =
	CreateButton("📜 QUEST",110)


local Attack =
	CreateButton("⚔ ATAQUE",165)


local Travel =
	CreateButton("🚢 VIAGEM",220)


local Config =
	CreateButton("⚙ CONFIG",275)



--==================================================
-- ABRIR / FECHAR
--==================================================

Bubble.MouseButton1Click:Connect(function()

	Bubble.Visible = false

	Panel.Visible = true

end)



Minimize.MouseButton1Click:Connect(function()

	Panel.Visible = false

	Bubble.Visible = true

end)



--==================================================
-- DRAG
--==================================================

DragObject(Bubble, Bubble)

DragObject(Header, Panel)



--==================================================
-- TESTE
--==================================================

Quest.MouseButton1Click:Connect(function()
	Status.Text = "📜 Quest"
end)


Attack.MouseButton1Click:Connect(function()
	Status.Text = "⚔ Ataque"
end)


Travel.MouseButton1Click:Connect(function()
	Status.Text = "🚢 Viagem"
end)


Config.MouseButton1Click:Connect(function()
	Status.Text = "⚙ Config"
end)



print("🐺 DOUBLEX V6 carregado")
